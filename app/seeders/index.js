const initialBarang = require('./barang_seeders');
const initialKasir = require('./kasir_seeders');
const initialTenan = require('./tenan_seeders');
const initialNota = require('./nota_seeders');
const initialBarangNota = require('./barang_nota_seeders');

module.exports = {
  initialBarang,
  initialKasir,
  initialTenan,
  initialNota,
  initialBarangNota,
};
